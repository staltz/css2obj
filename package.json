{
  "name": "css2obj",
  "version": "1.0.0",
  "description": "Tagged template literal that takes CSS and returns a JavaScript object suitable for free-style",
  "author": "staltz",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "pretest": "npm run lib",
    "test": "ava",
    "prelib": "mkdir -p lib",
    "lib": "tsc",
    "prepublish": "npm run lib"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "typescript": "^2.0.2"
  }
}
